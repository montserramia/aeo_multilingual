<?php

/**
 * @file
 * Hooks and callbacks for the AEO Multilingual module.
 */

use Drupal\Core\Routing\RouteMatchInterface;
use Drupal\Core\Url;

/**
 * Implements hook_help().
 */
function aeo_multilingual_help($route_name, RouteMatchInterface $route_match) {
  switch ($route_name) {
    case 'help.page.aeo_multilingual':
      $output = '<h3>' . t('About') . '</h3>';
      $output .= '<p>' . t('The AEO Multilingual module audits and optimizes each language version of your content independently for Answer Engine Optimization (AEO) and AI citations.') . '</p>';
      $output .= '<h3>' . t('Uses') . '</h3>';
      $output .= '<dl>';
      $output .= '<dt>' . t('Dashboard') . '</dt>';
      $output .= '<dd>' . t('View AEO scores for all content at <a href=":url">AEO Multilingual Dashboard</a>.', [':url' => Url::fromRoute('aeo_multilingual.dashboard')->toString()]) . '</dd>';
      $output .= '<dt>' . t('Per-node audit') . '</dt>';
      $output .= '<dd>' . t('Edit any node and click the "AEO Multilingual" tab.') . '</dd>';
      $output .= '</dl>';
      return $output;
  }
}

/**
 * Implements hook_theme().
 */
function aeo_multilingual_theme($existing, $type, $theme, $path) {
  return [
    'aeo_multilingual_dashboard' => [
      'variables' => [
        'languages' => [],
        'nodes' => [],
        'summary' => [],
      ],
      'template' => 'aeo-multilingual-dashboard',
    ],
    'aeo_multilingual_node_tab' => [
      'variables' => [
        'node' => NULL,
        'results' => [],
        'languages' => [],
      ],
      'template' => 'aeo-multilingual-node-tab',
    ],
  ];
}
